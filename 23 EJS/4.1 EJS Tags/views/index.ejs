<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EJS Tags</title>
</head>

<body>
  <h1>
    <%= locals.title %>
  </h1>
  <p>Current second:
    <%= locals.seconds %>
  </p>
  <% if (locals.seconds % 2===0) { %>
    <ul>
      <% locals.items.forEach((fruit)=> { %>
        <li>
          <%= fruit %>
        </li>
        <% }) %>
    </ul>
    <% } else { %>
      <p>No items to display</p>
      <% } %>
        <p>
          <%- locals.htmlContent %>
        </p>
        <%- include('footer.ejs') %>
</body>

</html>